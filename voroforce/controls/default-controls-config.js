export const defaultControlsConfig = {
  logging: true,
  autoFocusCenter: {
    enabled: 'touch', // true, false or 'touch'
    random: false,
  },
  maxSpeed: 10,
  // maxSpeedPinned: 5,
  ease: 0.15, // smoothing factor
  // easePinned: 0.05,
  freezeOnShake: {
    enabled: true,
  },
  freezeOnJolt: {
    enabled: true,
  },
}
